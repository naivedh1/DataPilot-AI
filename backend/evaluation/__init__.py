"""Offline evaluation harness.

Run from the `backend/` directory:

    python -m evaluation.run

Scores generated SQL on validity, execution success, answer correctness,
groundedness and latency against a fixed question set in `cases/`.
"""
